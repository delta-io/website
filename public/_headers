# Headers for this site are defined in netlify.toml ([[headers]]).
# Do not add Content-Security-Policy here — Netlify merges duplicate CSP
# values with commas, which breaks the policy. Kept as a stub so a stray
# public/_headers doesn't get reintroduced without context.
